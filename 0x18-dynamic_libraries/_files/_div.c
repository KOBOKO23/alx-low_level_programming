#include <stdio.h>

/**
 * _div - gives the result of dividing two integers.
 * @a: first integer.
 * @b: second integer.
 * Return: Always zero (success)
 */

int _div(int a, int b)
{
	return (a / b);
}

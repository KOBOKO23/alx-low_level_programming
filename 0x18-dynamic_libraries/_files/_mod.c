#include <stdio.h>

/**
 * _mod - gives the modulus of two integers.
 * @a: first integer.
 * @b: second integer.
 * Return: Remainder of dividing two numbers.
 */

int _mod(int a, int b)
{
	return (a % b);
}
